version: '3'

services:
  pazardan.com.tr:
    container_name: pazardan.com.tr
    image: pazardan.com.tr
    build:
      context: .
    ports:
      - 81:80
    expose:
      - 81
    networks:
      - internal

networks:
  internal:
    driver: bridge
